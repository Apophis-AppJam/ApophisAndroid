<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apophis_android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Request camera permissions -->
    <uses-feature android:name="android.hardware.type.watch" />
    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".ui.login.GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Apophis_android"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".ui.secondDay.SecondDayChatActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />
        <activity android:name=".ui.firstDay.CameraActivity" />
        <activity android:name=".ui.firstDay.CompassActivity" />
        <activity android:name=".ui.firstDay.FirstDayChatActivity" />

        <activity android:name=".MainActivity" />
        <activity android:name=".ui.secondDay.SecondDayTimepickerActivity" />
        <activity android:name=".ui.secondDay.SecondDayFindLightMeActivity" />
        <activity android:name=".ui.secondDay.SecondDayFindDarkMeActivity" />
        <activity android:name=".ui.secondDay.SecondDayValueActivity" />
        <activity android:name=".ui.secondDay.SecondDayValueResultActivity" />
    </application>

</manifest>